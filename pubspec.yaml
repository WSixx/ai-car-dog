name: cat_or_dog_ia
description: Flutter AI.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  percent_indicator: ^2.1.9+1
  image_picker: ^0.7.2+1
  tflite: ^1.1.1
  #flutter_web_image_picker: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/